# Mohamed Ikbel Said — Portfolio (React 19 + Vite + Tailwind)

A lightweight, ATS-friendly developer portfolio built with React 19, Vite, Tailwind, and React Router.

## Quick Start
```
npm install
npm run start
```

Place PDFs in `public/docs/` and edit `src/data/projects.js` to customize.
